export interface LanguageText {
  appName: string;
  layout: Layout;
}

export interface Layout {
  slogan: string;
  home: string;
  card: string;
  payments: string;
  credit: string;
  settings: string;
}
